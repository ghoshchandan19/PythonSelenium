city=" Kolkata   "
print(city.strip()) # reomves space from both the end
print(city.lstrip()) # reomves space from left end
print(city.rstrip()) # reomves space from right end

city.index()
city.find()
city.rfind()
city.rindex()